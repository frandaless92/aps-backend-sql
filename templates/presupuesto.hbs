<style>
  body { font-family: Arial, Helvetica, sans-serif; font-size: 13px; color:
  #000; margin: 0; padding: 0; } .border-box { padding: 25px; border-radius:
  12px; background: linear-gradient(to bottom, #d8ecff 0%, #ffffff 45%, #ffffff
  55%, #d8ecff 100%); border: 2px solid #bcd9f5; box-shadow: 0px 0px 12px
  rgba(0,0,0,0.08); box-sizing: border-box; } /* HEADER Y FOOTER REPETIBLES */
  thead { display: table-header-group; } tfoot { display: table-footer-group; }
  .header { display: flex; justify-content: space-between; margin-bottom: 20px;
  } .logo { width: 160px; } .cuadro-x { width: 55px; height: 55px; font-size:
  26px; border: 2px solid #000; display: flex; justify-content:center;
  align-items:center; margin: 0 20px; font-weight:bold; } .presupuesto-info {
  text-align:right; font-size: 13px; } table { width: 100%; border-collapse:
  collapse; margin-top: 12px; font-size: 12px; } th, td { border: 1px solid
  #444; padding: 6px; text-align:center; } th { background:#98dceb; font-size:
  13px; font-weight:bold; } .total { font-size: 18px; text-align:right;
  margin-top: 20px; font-weight:bold; } footer { font-size: 11px; margin-top:
  15px; text-align:center; } .page-break { page-break-before: always; }
</style>

<body>
  <div class="border-box">

    <div class="header">
      <img src="data:image/png;base64,{{logoBase64}}" class="logo" />

      <div class="cuadro-x">X</div>

      <div class="presupuesto-info">
        <strong>PRESUPUESTO Nº {{presupuestoNumero}}</strong><br />
        Fecha:
        {{fecha}}<br />
        Válido hasta:
        {{validez}}<br /><br />
        Trabajó:
        {{trabajo}}<br />
        Vendedor:
        {{vendedor}}<br />
        Condición de pago:
        {{condicionPago}}
      </div>
    </div>

    <strong>Datos del Cliente</strong>
    <div>
      {{cliente.apellido}},
      {{cliente.nombre}}<br />
      Empresa:
      {{cliente.empresa}}<br />
      Dirección:
      {{cliente.direccion}}<br />
      Teléfono:
      {{cliente.telefono}}<br />
      Email:
      {{cliente.email}}
    </div>

    <table>
      <thead>
        <tr>
          <th style="width:70px;">Cant.</th>
          <th>Descripción</th>
          <th style="width:120px;">Precio Unit.</th>
          <th style="width:120px;">Subtotal</th>
        </tr>
      </thead>

      <tbody>
        {{#each items}}
          <tr>
            <td>{{cantidad}}</td>
            <td>{{descripcion}}</td>
            <td>$ {{precio_unitario}}</td>
            <td>$ {{precio_total}}</td>
          </tr>
        {{/each}}
      </tbody>

      <tfoot>
        <tr>
          <td
            colspan="4"
            style="text-align:right; font-size:16px; font-weight:bold;"
          >
            TOTAL: $
            {{total}}
          </td>
        </tr>
      </tfoot>
    </table>

    <footer>
      * Valores en ARS.<br />
      * Validez según fecha indicada.<br />
      * Entrega estimada: 5 días.<br />
      * No incluye traslado ni instalación.<br /><br />
      <strong>¡GRACIAS POR SU CONFIANZA!</strong>
    </footer>

  </div>
</body>